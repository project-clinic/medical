<div class="container">
  <div class="row">
    <div class="col l10">
      <div class="row">
        <% if(isLoggedIn) { %>
          <form action="/logout" method="post">
            <button>Log out</button>
          </form>
        <% } else { %>
          <a href="/login">Log in</a>
        <% } %>
      </div>
    </div>
  </div>

</div>
