<ul>
  <% pathos.forEach(patho => { %>
    <li>
      <%= patho.name %>
      <a href="/<%= patho._id %>/report/new %>">New report</a>
    </li>
    <%  }) %>
  </ul>
  <a href="/<%= patient %>/report/new">New Report && Pathology</a>